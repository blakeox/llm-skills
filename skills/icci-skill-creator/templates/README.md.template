# {{Skill Name}}

> {{Origin prompt — the exact prompt (spell-checked) that the user gave Claude to create this skill. This is training material for the team. It shows how to ask Claude to build skills effectively. Include the full prompt, not a summary.}}

## Overview

{{2-3 sentences about what this skill does, who it's for, and what problem it solves.}}

## Prerequisites

- **Claude Code** installed
- **GitHub access** to the `icci` organization
{{Add skill-specific prerequisites:}}
- {{Required MCPs (e.g., HappyFox MCP enabled)}}
- {{Required tools (e.g., GAM installed at ~/bin/gam7/gam)}}
- {{Required credentials (e.g., DI API key — provided at session start)}}

## Installation

```bash
# If using symlink from icci-skills monorepo (recommended):
ln -s ~/Documents/GitHub/icci-skills/skills/{{skill-name}} ~/.claude/skills/{{skill-name}}

# If standalone repo:
git clone git@github.com:icci/{{skill-name}}.git ~/Documents/GitHub/{{skill-name}}
ln -s ~/Documents/GitHub/{{skill-name}} ~/.claude/skills/{{skill-name}}
```

## Usage

See **[USAGE.md](USAGE.md)** for detailed examples and recipes.

**Quick start:** {{One-sentence description of the most common invocation}}

## Changelog

See **[VERSION](VERSION)** for version history.
